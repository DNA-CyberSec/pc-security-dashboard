# 🛡️ PC Health & Security Dashboard

---

## English

### Description

A public SaaS product for monitoring, securing, and protecting Windows PCs. Any user can sign up with Google, connect their PC using the lightweight local agent, and view their security dashboard from any browser — fully isolated per user.

AI-powered recommendations are generated by Claude (Anthropic) via Firebase Functions — no API keys ever touch the browser. Each user's data is strictly isolated under their own Firestore path.

---

### Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        WEB BROWSER                          │
│              React App (Firebase Hosting)                   │
│   Login  ──►  Dashboard  ──►  Report + AI Recommendations  │
└───────────────────────┬─────────────────────────────────────┘
                        │ Firestore (real-time sync)
          ┌─────────────┴──────────────┐
          │      Firebase Cloud        │
          │  ┌─────────────────────┐   │
          │  │  Firebase Auth      │   │  Google Sign-In only
          │  │  (Google Sign-In)   │   │
          │  └─────────────────────┘   │
          │  ┌─────────────────────┐   │
          │  │  Firestore          │   │  scans, users, agents,
          │  │  (database)         │   │  pending_actions, logs
          │  └─────────────────────┘   │
          │  ┌─────────────────────┐   │
          │  │  Firebase Functions  │  │  Claude API calls
          │  │  (Node.js)          │   │  Business logic
          │  └─────────────────────┘   │
          └─────────────┬──────────────┘
                        │ Firestore (reads pending_actions, writes scan results)
┌───────────────────────┴─────────────────────────────────────┐
│                    WINDOWS PC (local)                        │
│              Python Agent (runs as .exe)                     │
│  Scanner  ──►  Backup  ──►  Cleaner  ──►  Privacy Scanner   │
└─────────────────────────────────────────────────────────────┘
```

**Data flow:**
1. Agent scans PC → writes results to `scans/{scanId}` in Firestore
2. `onScanCreated` Function triggers → calls Claude API → writes AI recommendations back to scan doc
3. Web app reads scan + recommendations from Firestore → displays dashboard
4. User requests cleanup → web app writes to `pending_actions` → agent polls and executes safely

---

### Tech Stack

| Layer | Technology |
|---|---|
| Web app | React 18, React Router 6, i18next |
| Auth | Firebase Authentication (Google Sign-In) |
| Database | Cloud Firestore |
| Hosting | Firebase Hosting |
| Backend | Firebase Functions (Node.js 20) |
| AI | Claude Sonnet (Anthropic SDK) |
| Local agent | Python 3.11, psutil, firebase-admin |
| Agent build | PyInstaller (.exe) |

---

### Project Structure

```
pc-security-dashboard/
├── web-app/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Dashboard.jsx      # Main dashboard
│   │   │   ├── Login.jsx          # Google Sign-In page
│   │   │   └── Report.jsx         # Scan report + AI recommendations
│   │   ├── components/            # Shared UI components (future)
│   │   ├── i18n/
│   │   │   ├── en.json            # English strings
│   │   │   ├── he.json            # Hebrew strings (RTL)
│   │   │   └── index.js           # i18next setup
│   │   ├── App.jsx                # Router + auth guard
│   │   ├── firebase.js            # Firebase initialization
│   │   └── index.js               # React entry point
│   ├── public/index.html
│   ├── .env.example               # Required env vars
│   └── package.json
├── agent/
│   ├── main.py                    # Agent entry point + scheduler
│   ├── modules/
│   │   ├── scanner.py             # Read-only system scanner
│   │   ├── cleaner.py             # Safe file cleanup
│   │   ├── backup.py              # Backup & restore (undo)
│   │   └── privacy.py             # Browser data scanner
│   ├── .env.example               # USER_ID + cred path
│   └── requirements.txt
├── functions/
│   ├── index.js                   # Firebase Function exports
│   ├── claude.js                  # Claude API wrapper
│   └── package.json
├── firestore.rules                # Security rules
├── firestore.indexes.json         # Composite indexes
├── firebase.json                  # Firebase project config
├── .firebaserc                    # Project alias
└── README.md
```

---

### Installation

#### Prerequisites
- Node.js 20+
- Python 3.11+
- Firebase CLI: `npm install -g firebase-tools`
- A Firebase project with Firestore, Auth, Hosting, and Functions enabled

---

#### Web App Setup

```bash
cd web-app
cp .env.example .env
# Fill in your Firebase config values in .env

npm install
npm start          # development
npm run build      # production build
```

Deploy to Firebase Hosting:
```bash
firebase deploy --only hosting
```

---

#### Firebase Functions Setup

Set your Anthropic API key as a Firebase secret:
```bash
firebase functions:secrets:set ANTHROPIC_API_KEY
```

Deploy functions:
```bash
firebase deploy --only functions
```

---

#### Local Agent Setup (Windows PC)

1. Download `serviceAccountKey.json` from:
   Firebase Console → Project Settings → Service Accounts → Generate New Private Key

2. Set up the agent:
```bash
cd agent
pip install -r requirements.txt
cp .env.example .env
# Edit .env:
#   FIREBASE_CRED_PATH=serviceAccountKey.json
#   USER_ID=<your Firebase UID from the web app after first sign-in>
```

3. Run the agent:
```bash
python main.py
```

4. Build as Windows .exe (optional):
```bash
pyinstaller --onefile --name pc-security-agent main.py
```

---

#### Firestore Setup

Deploy security rules and indexes:
```bash
firebase deploy --only firestore
```

Enable Google Sign-In:
Firebase Console → Authentication → Sign-in method → Google → Enable

---

### Usage Guide

1. Open the web app and sign in with your Google account
2. Download and run the local agent on the Windows PC you want to monitor
3. The agent automatically sends a heartbeat every 30 seconds — the dashboard shows "Agent Connected" when live
4. The agent runs an initial scan on startup and then every 60 minutes
5. Open the **Reports** page to see scan results + AI recommendations
6. Use **Preview** mode to review what would be cleaned before any action
7. Toggle off Preview mode to enable live cleanup (auto backup always created first)
8. Use **Undo** to restore files from backup if needed

---

### Safety Principles

| Principle | Implementation |
|---|---|
| **Read-only by default** | `READ_ONLY_MODE = True` in agent; all mutations require explicit opt-in |
| **Preview before deletion** | Report page shows all items in Preview mode; no changes made until confirmed |
| **Auto backup before cleanup** | `BackupManager.create_backup()` is always called before `Cleaner.clean_items()` |
| **Full undo capability** | Backup ZIP restored via `BackupManager.restore_backup()`; undo button in UI |
| **Protected paths** | `C:\Windows`, `C:\Program Files` and system roots are blocked in cleaner |
| **Recycle bin first** | `send2trash` moves files to recycle bin instead of permanent deletion on Windows |
| **Detailed audit log** | Every action (clean, backup, undo) written to `action_log` collection in Firestore |

---

### Auth & Data Rules

- **Google Sign-In only** — no passwords, no email/password auth
- Users can only access their own scan data (enforced in `firestore.rules`)
- Agent writes to Firestore via service account (Admin SDK) — not accessible from browser
- User schema includes `plan` field (`"free"` / `"pro"`) for future paid tiers

---

### Internationalization (i18n)

- Full **Hebrew** and **English** support
- Hebrew renders in **RTL** (`dir="rtl"`), English in **LTR**
- Language toggle button in all pages (persisted to `localStorage`)
- AI recommendations generated in the user's preferred language

---

## עברית

### תיאור הפרויקט

מערכת היברידית לניטור, ניקוי ואבטחת מחשב Windows. לוח הבקרה (React + Firebase) נגיש מכל דפדפן, בעוד שסוכן Python קל-משקל פועל מקומית על המחשב הנסרק ומתקשר עם Firebase בזמן אמת.

המלצות מופקות על ידי Claude (Anthropic) דרך Firebase Functions — מפתחות API לא נחשפים לדפדפן.

### ארכיטקטורה

- **אפליקציית ווב** (React): גישה מכל דפדפן, מציגה תוצאות סריקה והמלצות AI
- **סוכן מקומי** (Python .exe): פועל על המחשב הנסרק, שולח נתונים ל-Firestore
- **Firebase Functions**: מטפל בקריאות ל-Claude API ובלוגיקה עסקית
- **Firestore**: מאגר נתוני הסריקה, משתמשים ויומן פעולות

### עקרונות בטיחות

| עיקרון | יישום |
|---|---|
| **מצב קריאה בלבד כברירת מחדל** | כל שינוי דורש הסכמה מפורשת |
| **תצוגה מקדימה לפני מחיקה** | מציג את כל הפריטים לפני כל פעולה |
| **גיבוי אוטומטי לפני ניקוי** | גיבוי ZIP נוצר תמיד לפני מחיקה |
| **ביטול מלא** | שחזור מהגיבוי בלחיצה אחת |
| **נתיבים מוגנים** | `C:\Windows`, `C:\Program Files` חסומים |
| **יומן פעולות מפורט** | כל פעולה נרשמת ב-Firestore |

---

## Changelog

### [1.0.0] — 2026-03-21
**Step 10 complete: Multi-device adaptive dashboard — Grid View (1-5) + Command Center (6+)**
- **Multi-device Firestore architecture**: all data now under `/users/{uid}/devices/{deviceId}/` — scans, realtime, security, status
- **Agent deviceId**: each agent instance generates a persistent UUID on first run (saved to `config.json` / `device_id.txt`)
- **Dashboard — Grid View** (≤5 devices): full device cards with 4 mini gauges (CPU/RAM/Disk/Health), security summary, firewall grade, temps, live flash on new data
- **Dashboard — Command Center** (6+ devices): summary tiles (total/online/alerts/avg health), filter bar (All/Online/Offline/Alerts + search), sortable table with health bars and live CPU/RAM columns
- **Auto-mode switching**: automatically selects Grid or Table based on device count; manual toggle button always visible
- **Device nickname editing**: inline pencil → text field → save to Firestore — works in both grid and table view
- **Single Device View** (`/device/:deviceId`): full gauges dashboard with breadcrumb "← All Devices / DEVICENAME", back to multi-device list
- **Report page**: now reads from device-scoped Firestore path, passes `deviceId` to AI recommendations callable
- **Fully bilingual**: all new strings added to `en.json` and `he.json` including RTL support
- **Firestore rules**: new device-scoped rules — device doc writable by owner (for nickname), sub-collections (realtime/security) written only by Cloud Functions

### [0.1.0] — 2026-03-21
**Initial release — project scaffold**
- Full project structure created (web-app, agent, functions)
- React app with Login, Dashboard, and Report pages
- Hebrew/English i18n with RTL support
- Firebase Auth (Google Sign-In), Firestore, Hosting, Functions configured
- Local Python agent with Scanner, Cleaner, Backup, Privacy modules
- Safety principles enforced: read-only mode, preview, auto-backup, undo, audit log
- Claude AI integration via Firebase Functions (getScanRecommendations, explainIssue, onScanCreated trigger)
- Firestore security rules: users can only access their own data
- Firestore indexes for scans, pending_actions, action_log collections

### [0.2.0] — 2026-03-21
**Step 1 complete: Project structure created + Firebase connected**
- Firebase project `pc-security-dashboard` linked
- Web app registered and configured
- `web-app/.env` populated with real Firebase config (gitignored)
- `.firebaserc` updated with correct project ID
- Firestore database created (us-central1, Native mode)
- Firestore security rules deployed — users can only access their own data
- Firestore composite indexes deployed (scans, pending_actions, action_log)
- Firebase Hosting provisioned at `https://pc-security-dashboard.web.app`
- Google Sign-In enabled via Firebase Console
- Blaze billing plan activated

### [0.3.0] — 2026-03-21
**Step 2 complete: Firebase fully connected — Auth, Firestore, Hosting, Functions deployed**
- ANTHROPIC_API_KEY stored as Firebase secret (secretmanager, not in code)
- All 3 Cloud Functions deployed to us-central1 (Node.js 20, 2nd Gen):
  - `getScanRecommendations` — callable, returns AI analysis of a scan
  - `explainIssue` — callable, explains a specific vulnerability in plain language
  - `onScanCreated` — Firestore trigger, auto-generates AI recommendations on new scan
- Web app running locally at `http://localhost:3001`
- ESLint warnings resolved (react-hooks/exhaustive-deps)
- `pcguard-rami.web.app` created as dedicated Hosting site
- `pcguard-rami.web.app` added to Firebase Auth authorized domains

### [0.4.0] — 2026-03-21
**Step 3 complete: Web app live, Google Auth working, Windows Agent built**
- Web app live at `https://pcguard-rami.web.app` — Google Sign-In working
- Footer added: version number + Rami Hacmon / DNACybersec credit
- Git repository initialized; all files committed
- **Windows Agent fully rebuilt (v0.4.0) — 5 modules, READ-ONLY:**
  - `scanner.py` — all drives disk usage, temp file summary, large files (>500MB),
    startup items (folder + registry), installed software (from registry uninstall keys),
    security vulnerabilities (Defender, Firewall, UAC, pending Windows Updates)
  - `processes.py` — running process list, CPU/memory per process,
    suspicious flag (known malware names, temp-path execution, high resource usage)
  - `network.py` — active TCP/UDP connections, process attribution,
    suspicious flag (RAT ports, unexpected outbound, temp-process network activity)
  - `privacy.py` — browser cookie/history/cache counts (Chrome, Edge, Firefox)
  - `backup.py` / `cleaner.py` — kept for future write-mode use, blocked in read-only
- Heartbeat interval: 60 seconds
- Health score algorithm: weighted deductions for disk >80%, vulns, suspicious processes/connections

### [0.5.0] — 2026-03-21
**Step 4 complete: Agent built — scanner, processes, network, health score**
- `agent/.env.example` updated with all required variables (FIREBASE_CRED_PATH, USER_ID, SCAN_INTERVAL, HEARTBEAT_INTERVAL)
- `SCAN_INTERVAL` and `HEARTBEAT_INTERVAL` now configurable from `.env`
- `install.bat` created for one-click Windows setup:
  - Checks for Python, creates `C:\pc-security-agent\`, copies files, installs dependencies
  - Validates presence of `.env` and AgentToken, offers to start immediately

### [0.6.0] — 2026-03-21
**Step 5 complete: Redesigned as public multi-user SaaS product**
- **Architecture change:** agent no longer uses service account or Admin SDK
  - Agent authenticates via a per-user `AgentToken` (format: `pcg-...`)
  - Agent calls Cloud Functions HTTP endpoints (`agentHeartbeat`, `submitScan`)
  - No Firebase credentials needed — just the AgentToken in `.env`
- **New Cloud Functions:** `generateAgentToken`, `agentHeartbeat`, `submitScan`
- **Firestore restructured:** all user data moved to `/users/{uid}/` subcollections
  - `/users/{uid}/scans/{scanId}` — scan results
  - `/users/{uid}/agent/status` — heartbeat + agent metadata
  - `/agentTokens/{token}` — server-side only, maps token → uid
- **Firestore rules:** strict per-user isolation, `agentTokens` not readable by clients
- **New Setup page** (`/setup`): shows AgentToken with copy button, download link, step-by-step instructions
- **Dashboard** updated: "Set Up Agent" button links to `/setup`
- **Agent simplified:** removed `firebase-admin` dependency, now uses `requests` only
- `agent/.env.example` simplified to just `AGENT_TOKEN`, `SCAN_INTERVAL`, `HEARTBEAT_INTERVAL`
- `install.bat` updated: validates AgentToken, no service account required

### [0.8.0] — 2026-03-21
**Step 7 complete: GitHub Actions CI/CD — auto build .exe + auto deploy web app**
- `.github/workflows/build-agent.yml` — runs on `windows-latest`:
  - Triggers on every push to `main` (and manual trigger)
  - Installs Python 3.11 + agent dependencies
  - Builds `PCGuard-Setup.exe` via PyInstaller (`--onefile --windowed`)
  - Uploads artifact + creates/updates GitHub Release with download link
  - Release notes include plain-English install instructions
- `.github/workflows/deploy-web.yml` — runs on `ubuntu-latest`:
  - Triggers on every push to `main` (and manual trigger)
  - Installs Node.js 20 + builds React app with production Firebase env vars
  - Deploys to Firebase Hosting site `pcguard-rami` via `FirebaseExtended/action-hosting-deploy`
- Required GitHub secrets: `FIREBASE_SERVICE_ACCOUNT`, `REACT_APP_FIREBASE_API_KEY`, `REACT_APP_FIREBASE_AUTH_DOMAIN`, `REACT_APP_FIREBASE_PROJECT_ID`, `REACT_APP_FIREBASE_STORAGE_BUCKET`, `REACT_APP_FIREBASE_MESSAGING_SENDER_ID`, `REACT_APP_FIREBASE_APP_ID`

### [0.7.0] — 2026-03-21
**Step 6 complete: Non-technical user flow — single .exe installer with GUI wizard**
- `agent/setup_wizard.py` — dual-mode entry point for the bundled .exe:
  - GUI mode (default): tkinter 3-step wizard — welcome → installing → done
  - Background mode (`--run`): silent agent loop, no window, logs to file
  - Validates AgentToken against server before installing
  - Copies .exe to `C:\pc-security-agent\`, writes `config.json` (token + intervals)
  - Adds to Windows registry startup (HKCU, no admin rights needed)
  - Launches agent immediately after install
- `agent/build.bat` — one-click PyInstaller build → `dist/PCGuard-Setup.exe`
- `web-app/src/pages/Setup.jsx` — complete non-technical redesign:
  - Hero section: "Connect Your PC in 4 Easy Steps"
  - Step 1: token with big copy button
  - Step 2: single large download button
  - Step 3: 3 plain-English bullet points (double-click → paste → click Install)
  - Step 4: "Come back here — you're done!" with dashboard button
  - Full Hebrew/English support
