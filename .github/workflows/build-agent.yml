name: Build Agents

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Sync version into agent/VERSION
        shell: bash
        run: |
          VERSION=$(node -p "require('./web-app/package.json').version")
          echo -n "$VERSION" > agent/VERSION
          echo "Synced agent/VERSION to $VERSION"

      - name: Install dependencies
        run: |
          cd agent
          pip install -r requirements.txt

      - name: Build PCGuard-Setup.exe
        run: |
          cd agent
          pyinstaller `
            --onefile `
            --windowed `
            --name "PCGuard-Setup" `
            --paths . `
            --add-data "VERSION;." `
            --hidden-import=psutil `
            --hidden-import=requests `
            --hidden-import=schedule `
            --hidden-import=dotenv `
            --hidden-import=winreg `
            --hidden-import=pystray `
            --hidden-import=pystray._win32 `
            --hidden-import=PIL `
            --hidden-import=PIL.Image `
            --hidden-import=PIL.ImageDraw `
            --hidden-import=modules.scanner `
            --hidden-import=modules.processes `
            --hidden-import=modules.network `
            --hidden-import=modules.privacy `
            --hidden-import=modules.backup `
            --hidden-import=modules.cleaner `
            --hidden-import=modules.network_info `
            setup_wizard.py

      - name: Generate Windows checksum
        shell: python
        run: |
          import hashlib, pathlib
          exe = pathlib.Path("agent/dist/PCGuard-Setup.exe")
          digest = hashlib.sha256(exe.read_bytes()).hexdigest()
          pathlib.Path("agent/dist/PCGuard-Setup.exe.sha256").write_text(digest + "\n")
          print(f"SHA256: {digest}")

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: PCGuard-Setup
          path: |
            agent/dist/PCGuard-Setup.exe
            agent/dist/PCGuard-Setup.exe.sha256

      - name: Get version from package.json
        id: version
        shell: bash
        run: echo "VERSION=$(node -p "require('./web-app/package.json').version")" >> $GITHUB_OUTPUT

      - name: Create GitHub Release (Windows)
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ steps.version.outputs.VERSION }}
          name: PCGuard v${{ steps.version.outputs.VERSION }}
          allowUpdates: true
          artifacts: "agent/dist/PCGuard-Setup.exe,agent/dist/PCGuard-Setup.exe.sha256"
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            ## PCGuard v${{ steps.version.outputs.VERSION }}

            ### Windows Installation
            1. Download **PCGuard-Setup.exe**
            2. Double-click the file
            3. Paste your personal token from the [Setup page](https://pcguard-rami.web.app/setup)
            4. Click **Install** — done!

            ### Linux / VPS Installation
            Run on your server as root:
            ```bash
            curl -sSL https://pcguard-rami.web.app/install.sh | bash
            ```

            > Windows 10/11 · Ubuntu 18+ · Debian 10+ · No technical knowledge needed

      - name: Update latestAgentVersion in Firestore
        shell: bash
        env:
          DEPLOY_SECRET: ${{ secrets.DEPLOY_SECRET }}
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          echo "Updating Firestore version to ${VERSION}..."
          RESPONSE=$(curl -sS -w "\n%{http_code}" -X POST \
            "https://us-central1-pc-security-dashboard.cloudfunctions.net/setLatestAgentVersion" \
            -H "Content-Type: application/json" \
            -d "{\"secret\":\"${DEPLOY_SECRET}\",\"windows_version\":\"${VERSION}\",\"linux_version\":\"${VERSION}\",\"mac_version\":\"${VERSION}\",\"changelog\":\"PCGuard v${VERSION}\"}")
          HTTP_CODE=$(echo "${RESPONSE}" | tail -1)
          BODY=$(echo "${RESPONSE}" | head -1)
          echo "Response body: ${BODY}"
          echo "HTTP status:   ${HTTP_CODE}"
          if [ "${HTTP_CODE}" != "200" ]; then
            echo "::error::setLatestAgentVersion returned HTTP ${HTTP_CODE}"
            exit 1
          fi

  build-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync version into agent-linux/VERSION
        run: |
          VERSION=$(node -p "require('./web-app/package.json').version")
          echo -n "$VERSION" > agent-linux/VERSION
          echo "Synced agent-linux/VERSION to $VERSION"

      - name: Package Linux agent
        run: |
          mkdir -p dist-linux
          cp agent-linux/linux_agent.py dist-linux/
          cp agent-linux/requirements-linux.txt dist-linux/
          cp agent-linux/VERSION dist-linux/
          tar -czf PCGuard-Linux.tar.gz -C dist-linux .

      - name: Generate Linux checksum
        run: sha256sum PCGuard-Linux.tar.gz | awk '{print $1}' > PCGuard-Linux.tar.gz.sha256

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: PCGuard-Linux
          path: |
            PCGuard-Linux.tar.gz
            PCGuard-Linux.tar.gz.sha256

      - name: Get version from package.json
        id: version
        run: echo "VERSION=$(node -p "require('./web-app/package.json').version")" >> $GITHUB_OUTPUT

      - name: Add Linux package to GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ steps.version.outputs.VERSION }}
          name: PCGuard v${{ steps.version.outputs.VERSION }}
          allowUpdates: true
          artifacts: "PCGuard-Linux.tar.gz,PCGuard-Linux.tar.gz.sha256"
          token: ${{ secrets.GITHUB_TOKEN }}
          omitBody: true

      - name: Update latestAgentVersion in Firestore
        env:
          DEPLOY_SECRET: ${{ secrets.DEPLOY_SECRET }}
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          echo "Updating Firestore version to ${VERSION}..."
          RESPONSE=$(curl -sS -w "\n%{http_code}" -X POST \
            "https://us-central1-pc-security-dashboard.cloudfunctions.net/setLatestAgentVersion" \
            -H "Content-Type: application/json" \
            -d "{\"secret\":\"${DEPLOY_SECRET}\",\"windows_version\":\"${VERSION}\",\"linux_version\":\"${VERSION}\",\"mac_version\":\"${VERSION}\",\"changelog\":\"PCGuard v${VERSION}\"}")
          HTTP_CODE=$(echo "${RESPONSE}" | tail -1)
          BODY=$(echo "${RESPONSE}" | head -1)
          echo "Response body: ${BODY}"
          echo "HTTP status:   ${HTTP_CODE}"
          if [ "${HTTP_CODE}" != "200" ]; then
            echo "::error::setLatestAgentVersion returned HTTP ${HTTP_CODE}"
            exit 1
          fi

  build-mac:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync version into agent-mac/VERSION
        run: |
          VERSION=$(node -p "require('./web-app/package.json').version")
          echo -n "$VERSION" > agent-mac/VERSION
          echo "Synced agent-mac/VERSION to $VERSION"

      - name: Package macOS agent
        run: |
          mkdir -p dist-mac
          cp agent-mac/mac_agent.py    dist-mac/
          cp agent-mac/requirements.txt dist-mac/
          cp agent-mac/VERSION          dist-mac/
          tar -czf PCGuard-Mac.tar.gz -C dist-mac .

      - name: Generate macOS checksum
        run: sha256sum PCGuard-Mac.tar.gz | awk '{print $1}' > PCGuard-Mac.tar.gz.sha256

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: PCGuard-Mac
          path: |
            PCGuard-Mac.tar.gz
            PCGuard-Mac.tar.gz.sha256

      - name: Get version from package.json
        id: version
        run: echo "VERSION=$(node -p "require('./web-app/package.json').version")" >> $GITHUB_OUTPUT

      - name: Add macOS package to GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ steps.version.outputs.VERSION }}
          name: PCGuard v${{ steps.version.outputs.VERSION }}
          allowUpdates: true
          artifacts: "PCGuard-Mac.tar.gz,PCGuard-Mac.tar.gz.sha256"
          token: ${{ secrets.GITHUB_TOKEN }}
          omitBody: true

      - name: Update latestAgentVersion in Firestore
        env:
          DEPLOY_SECRET: ${{ secrets.DEPLOY_SECRET }}
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          echo "Updating Firestore version to ${VERSION}..."
          RESPONSE=$(curl -sS -w "\n%{http_code}" -X POST \
            "https://us-central1-pc-security-dashboard.cloudfunctions.net/setLatestAgentVersion" \
            -H "Content-Type: application/json" \
            -d "{\"secret\":\"${DEPLOY_SECRET}\",\"windows_version\":\"${VERSION}\",\"linux_version\":\"${VERSION}\",\"mac_version\":\"${VERSION}\",\"changelog\":\"PCGuard v${VERSION}\"}")
          HTTP_CODE=$(echo "${RESPONSE}" | tail -1)
          BODY=$(echo "${RESPONSE}" | head -1)
          echo "Response body: ${BODY}"
          echo "HTTP status:   ${HTTP_CODE}"
          if [ "${HTTP_CODE}" != "200" ]; then
            echo "::error::setLatestAgentVersion returned HTTP ${HTTP_CODE}"
            exit 1
          fi
