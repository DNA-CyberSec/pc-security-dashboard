# Path to Firebase service account key JSON
FIREBASE_CRED_PATH=serviceAccountKey.json

# Firebase user UID (copy from web app after first sign-in)
# Found in Firebase Console > Authentication > Users
USER_ID=paste_your_firebase_uid_here
